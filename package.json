{
  "name": "polumenta-js",
  "version": "1.0.0",
  "description": "JS Clone of Polumenta generator",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules",
    "start": "npm run build && node dist/index.js"
  },
  "author": "tbrnslv",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
